r2dbc:
  datasources:
    default:
      url: r2dbc:postgresql://localhost:5433/blockchain_db
      username: postgres
      password: 'postgres'
      schema-generate: CREATE_DROP
      dialect: POSTGRES
